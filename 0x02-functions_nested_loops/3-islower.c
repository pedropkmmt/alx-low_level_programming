#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 *int_islower - checks if c is lower
 *Return: always returns 0;
 */
int _islower(int c)
{
	int low = 'c';
	if (islower(low)
		_putchar(1);
	else 
		_putchar(0);
	return (0);
}
